# Add any local environment variables here for development
# Vite requires env vars to start with VITE_ to expose to client
# For example:
# VITE_API_BASE_URL=http://localhost:5000
# Stripe publishable key for client-side Checkout (do NOT commit your secret key)
# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_... 